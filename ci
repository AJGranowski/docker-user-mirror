#!/bin/sh
THIS_SCRIPT_DIR="$(dirname "$0" | xargs realpath)";
(cd "$THIS_SCRIPT_DIR" && ./matrix-compose-engine matrix-images matrix-test-suite run-test) && echo 'All test pass!' || (echo 'Test failure.' >&2 && false);